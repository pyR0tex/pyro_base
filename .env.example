# Copy to .env.local for local use. Never commit real credentials.
# Add these same server-only variables in Vercel > Project > Settings > Environment Variables.
RESEND_API_KEY=
CONTACT_EMAIL=
CONTACT_FROM_EMAIL=
